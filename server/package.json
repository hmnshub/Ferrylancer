{
  "name": "ferrylance-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Node.js/Express backend for Ferrylance — handles heavy tasks (image processing, feed ranking, search, notification fan-out) that sit next to Supabase (which continues to handle Auth + simple CRUD directly from the frontend).",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "helmet": "^7.1.0",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.33.5"
  }
}
